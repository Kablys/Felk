int factorial(int d,int h)[
  int c := (100*10*5+10/5);
  /*int pirmas := h(q,z);
  int antras := a+function(b);
  int xz:= b*f(b,a(a,h))*d;
  int c := 100*10*5+10/5;
  int div:=100/10*5;
  int a:= a*b+c<d; //
  int same:= a+(d*(c*e))*y;// sutvarkyti
  int a:= function(a(e,f()));
  float c:= 10.00;
  string a:= "some";
  int a := f()<d;
  int c := (100+10)*1+2*(1+2);//*5+10/5;
  int a := f(a,b(),b());*/
  /*systemOut("mes esame cia");
  int a := "Enter a number to calculate it's factorial\n";
  int as := a+function(c());
  int some:= a+function(a,function2());
  int a := b + function(c,d,a,e(),f);
  int a:= 100/10*5+2;
  int a := function(a,b);
  int b:= (a*b)+c+d*f;
  int a:= b*f(b,a(a,h))*d;
  systemIn(number);
  return a;*/
  while (a>b) [
    int a;
  ]
  //xx(12);
  char a := "a";
  if (a<b) [
      int ifEntered;
  ]
  else[
  int elseBlock;
  ]
  if (c<s) [
  ]
  int c;
  int result := 2;
 
  for c := x to n[
    result := result * c+1;
  ]
  return result;
]

int somethin()[
int c;
]

main()[
  int vienas := (((2+3)));
  int test := a;
  systemOut(a+"");
  int a := "asdasda";
  int name := (a<b)*(c + d);
  int h := b + a < C+d;
  int a := b*c + a;
  //int some :=  + a;
  int number := (1 + 2)*4;
  //int some := (1+2)*3*4;
  int fact := (1*2*3) + (2*4);
  systemOut("Enter a number to calculate it's factorial\n");
  systemOut(a+b);
  for c:= a to b [
  int forloop;
  ]
  systemIn(number);
  return a;
]
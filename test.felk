int factorial(int d,int h)[
  factorial();
  int f := factorial(1,2);
  float c:= 10.00;
  string a:= "some";
  int a := f(a,b);
  int c := (100*10*5+10/5);
  int a := f(a,b(),b());
  systemOut("mes esame cia");
  int a := ("Enter a number to calculate it's factorial\n");
  int as := a+function(b,c());
  int some:= a+function(ab,function2());
  int a := b + function(cd,ae(),f);
  int a:= 100/10*5+2;

  int b:= (a*b)+c+d*f;
  int a:= b*f(b,a(a,h))*d;
  systemIn(a);
  //return a;*/
  /*while a>b [
    int a;
    ];
//xx(1+2);
    a := "a";
  if a<b [
  int ifEntered;
  ]
  else[
  int elseBlock;
  ]
  if c<s [
  ]
  int c;
  int result := 2;
 
  for c := x to n[
    result := result * c+1;
  ]
  return result;*/
]

int somethin()[
factorial();
int c;
]

main()[
  /*int test := a;
  //systemOut(a+"");
  int a := ("asdasda");
  int name := (a<b)*(c + d);
  int h := b + a < C+d;
  int a := b*c + a;
  //int some :=  + a;
  int number := (1 + 2)*4;
  int some := (1+2)*3*4;
  int fact := (1*2*3) + (2*4);
  systemOut("Enter a number to calculate it's factorial\n");
  systemOut(a+b);
  for c:= a to b [
  int forloop;
  ]
  systemIn(number);
*/
  return a;
]
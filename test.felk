int factorial(int n, float x)[
    return 0 + true + 1.2 + a;
  while a>b [
    int a;
    ]

  if a<b [
  int ifEntered;
  ]
  else[
  int elseBlock;
  ]
  if c<s [
  ]
  int c;
  int result := 2;
 
  for c := 1 to n[
    result := result * c;
 ]
  return result;
]

int somethin()[
int c;
]
 
main()[
  //int name := (a+b)*c;
  int some := 2;
  int number := (1 + 2)*4;
  //int some := 1*b+c+(d*e*f)-g-h/i-j;
  //int fact := (1*2*3)+(2 * 4);
  //systemOut("Enter a number to calculate it's factorial\n");
  //systemIn("%d", &number);
  //systemOut("%d! = %ld\n", number, factorial(number)); //reikia sutvarkyti funkciju kvietima kaip parametra
 
  return;
]
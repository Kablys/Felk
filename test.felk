/*int factorial(int n, float x)[
  systemOut("mes esame cia");
  int := "Enter a number to calculate it's factorial\n";
  systemIn(number+1);
  while a>b [
    int a;
    ]

  if a<b [
  int ifEntered;
  ]
  else[
  int elseBlock;
  ]
  if c<s [
  ]
  int c;
  int result := 2;
 
  for c := 1 to n[
    result := result * c+1;
  ]
  return result;
]

int somethin()[
int c;
]
*/
main()[
int a := (((a+b)));
int a := ("asdasda");
  int name := (a<b)*c + d;
  int h := b + a < C+d;
  int a := b*c + a;
  int some :=  + a;
  int number := (1 + 2)*4;
  int some := (1+2)*3*4*; //should be an error
  int fact := (1*2*3) + (2*4);
  systemOut("Enter a number to calculate it's factorial\n");
  systemOut(a+b);
  for c:= a to b [
  int forloop;
  ]
  systemIn(number);

  return a;
]